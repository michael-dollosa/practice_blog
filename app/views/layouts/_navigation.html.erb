<div class="app-label">
  <%= link_to 'Chismis', article_path %>
</div>

<div class="app-options">
  <% if is_logged_in? %>
    <%= link_to 'Logout', logout_path, method: :delete %>
  <% else %>
    <%= link_to 'Login', login_path %>
    <%= link_to 'Create Account', user_new_path %>
  <% end %>
</div>

